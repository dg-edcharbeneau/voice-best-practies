{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Deepgram": {
    "//": "DEEPGRAM_API_KEY is read from the environment or user-secrets, NEVER from here (don't commit secrets).",
    "TokenTtlSeconds": 60
  }
}
