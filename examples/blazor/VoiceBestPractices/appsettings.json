{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Deepgram": {
    "//": "DEEPGRAM_API_KEY is read from the environment or user-secrets, NEVER from here (don't commit secrets).",
    "TokenTtlSeconds": 60
  },
  "OpenAI": {
    "//": "Optional. Set OpenAI:ApiKey via env/user-secrets to enable the /api/chat LLM variant (echo is the default). Endpoint is optional (Azure OpenAI or any OpenAI-compatible service).",
    "Model": "gpt-4o-mini"
  }
}
