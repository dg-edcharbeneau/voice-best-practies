@inherits LayoutComponentBase

@* Minimal single-column shell — the demo is one page. All visual styling lives
   in the shared, framework-agnostic app.css (the same [data-state]-driven
   stylesheet as the vanilla and React examples). *@

@Body

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
